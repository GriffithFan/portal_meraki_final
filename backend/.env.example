# ============================================
# Portal Meraki - Variables de Entorno
# ============================================
# Copia este archivo a .env y configura los valores.
# Ver ENV_DOCUMENTATION.md para descripción detallada.
# ============================================

# ============================================
# SERVIDOR
# ============================================

# Entorno de ejecución (development | production)
NODE_ENV=development

# Puerto del servidor backend
PUERTO=3000

# Host para binding (0.0.0.0 permite acceso externo)
HOST=0.0.0.0

# Número de proxies de confianza (para X-Forwarded-For)
TRUST_PROXY_HOPS=1

# ============================================
# API CISCO MERAKI (REQUERIDO)
# ============================================

# Tu API Key de Meraki Dashboard (REQUERIDO)
# Obtener en: https://dashboard.meraki.com > Organization > Settings > Dashboard API access
MERAKI_API_KEY=

# ID de organización específica (OPCIONAL)
# Si no se define, el sistema recorre todas las organizaciones
MERAKI_ORG_ID=

# URL base de la API Meraki (no cambiar a menos que sea necesario)
# MERAKI_BASE_URL=https://api.meraki.com/api/v1

# ============================================
# SEGURIDAD Y AUTENTICACIÓN (REQUERIDO EN PRODUCCIÓN)
# ============================================

# Clave secreta para firmar tokens JWT (REQUERIDO)
# Usar al menos 32 caracteres aleatorios
JWT_SECRETO=

# Clave para endpoints administrativos (REQUERIDO)
# Se recomienda hash SHA-256 (64 caracteres hex)
# Usada en header: x-admin-key: <valor>
ADMIN_KEY=

# Clave secundaria de admin (OPCIONAL, para testing)
# SECOND_ADMIN_KEY=

# ============================================
# CORS - ACCESO REMOTO
# ============================================

# Orígenes permitidos para CORS (separados por comas)
# En desarrollo: cualquier origen es permitido si NODE_ENV != production
# Usar '*' permite cualquier origen (NO recomendado en producción)
CORS_ORIGINS=http://localhost:5173

# ============================================
# CACHÉ Y OPTIMIZACIÓN
# ============================================

# Habilitar precarga de caché al iniciar (true | false)
ENABLE_WARM_CACHE=true

# Cantidad de predios frecuentes a precargar en caché
WARM_CACHE_SIZE=20

# TTL de caché LLDP en milisegundos (default: 10 minutos)
# LLDP_CACHE_TTL_MS=600000

# ============================================
# ACTUALIZACIÓN DE PREDIOS
# ============================================

# Intervalo de actualización automática de predios (minutos)
# PREDIOS_REFRESH_INTERVAL_MINUTES=30

# Delay inicial antes de primera actualización (ms)
# PREDIOS_REFRESH_INITIAL_DELAY_MS=30000

# ============================================
# PERFORMANCE (Node.js)
# ============================================

# Tamaño del pool de threads para operaciones I/O
# UV_THREADPOOL_SIZE=16

# Memoria máxima asignada a Node.js (en MB)
# NODE_OPTIONS=--max-old-space-size=1024
